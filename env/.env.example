# .env.example file
# Copy this file to .env and fill in the values only in your local environment
# DO NOT upload the .env with real values to the repository.

# Database (if your deployment requires it)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=dvwa
DB_USER=dvwa_user
DB_PASS=changeme

# DVWA / App config (example)
DVWA_SECURITY=low

# Optional: host port (if you want to override)
HOST_HTTP_PORT=8080
